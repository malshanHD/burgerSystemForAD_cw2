@model IEnumerable<burgerSystem.Models.burger_type>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.typeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.typeImage)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.typeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.typeImage)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.typeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.typeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.typeID })
        </td>
    </tr>
}

</table>
